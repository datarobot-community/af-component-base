---
name: "{{ template_code_name }}"
description: |-
  {{ template_description }}
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
  datarobot:tracecontext: "{{ template_code_name }}"
